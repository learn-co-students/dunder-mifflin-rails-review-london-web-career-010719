<h1><%= @dog.name %></h1>

  <p> Breed: <%= @dog.breed %></p>
  <p> Age: <%= @dog.age %></p>


<h2> My Employees </h2>
  <ul>
    <% @dog.employees.each do |e| %>
      <li><%= link_to e.first_name, employee_path(e) %></li>
    <% end %>
  </ul>
