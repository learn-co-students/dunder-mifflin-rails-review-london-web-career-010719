<h1>Employees<h1>

 <ul>
  <% @employees.each do |e| %>
    <li><%= link_to e.first_name, employee_path(e) %></li>
    <% end %>
  </ul>

  <%= link_to '<button type="button">Create New Employee</button>'.html_safe, new_employee_path %>
